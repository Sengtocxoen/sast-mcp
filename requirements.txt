# ============================================================================
# MCP-SAST-Server Requirements
# ============================================================================
# Versions are pinned to what was tested. Bump deliberately, not automatically.
# ============================================================================

# Web Framework
flask==3.0.3
flask-cors==4.0.1

# HTTP client (MCP client + results API)
requests==2.31.0
aiohttp==3.9.5

# Model Context Protocol
mcp>=0.9.0

# Environment variables
python-dotenv==1.0.1

# Process and system monitoring
psutil==5.9.8

# ============================================================================
# NOTE: python-toon was removed — the package does not exist on PyPI.
# TOON conversion is handled internally via tools/toon_converter.py and
# gracefully disabled when the import fails.
# ============================================================================

# ============================================================================
# Optional Python-based SAST tools (uncomment to install via pip)
# Most tools are installed separately via apt/npm/go.
# ============================================================================
# bandit==1.7.8
# safety==3.2.0
# checkov==3.2.0
